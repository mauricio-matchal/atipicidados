version: '3.9'

services:
  db:
    container_name: db
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - app-network

  backend:
    container_name: backend
    image: backend
    build:
      context: ../backend
      dockerfile: backend.dockerfile
    ports:
      - "3002:3002"
    environment:
      - DATABASE_URL=postgresql://atipicidades_yaht_user:V0IbI742UWIBt5LaOjNKPHEG3Zkrvlqn@dpg-crtinrdds78s73er52gg-a.oregon-postgres.render.com/atipicidades_yaht
    depends_on:
      - db
    networks:
      - app-network

  pgadmin:
    container_name: pgadmin
    image: dpage/pgadmin4:latest
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8080:80"
    depends_on:
      - db
    networks:
      - app-network

volumes:
  pgdata:

networks:
  app-network:
    driver: bridge
